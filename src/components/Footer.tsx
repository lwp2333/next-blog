import { FC } from 'react'

interface Props {}

const Footer: FC<Props> = props => {
  return <div className=" fixed left-0  bottom-0 flex h-8 w-screen items-center justify-center bg-white">Footer</div>
}

export default Footer
